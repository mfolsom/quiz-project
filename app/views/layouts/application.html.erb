<!DOCTYPE html>
<html>
<head>
  <title>QuizProject</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= yield (:body_class) %>">
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, class: name %>
  <% end %>

  <%= yield %>
  <footer>
    <ul class="user-links left">
      <%if current_user %>    
        <li><%= link_to "#{current_user.username}'s Profile", user_path(current_user.id) %></li>
        <li><a href="#">Stats</a></li>
      <% else %>
        <li><%= link_to "Sign In", sign_in_path %></li>
      <% end %>
    </ul>
    <a href="/" id="logo">QuizApp</a>
    <ul class="user-links right">
      <%if current_user%>
        <li><%= link_to "Sign Out", sign_out_path, method: 'delete' %></li>
        <li><%= link_to "Add Question", new_question_path %></li>
      <% else %>
        <li><%= link_to "Sign Up", new_user_path %></li>
      <% end %>
    </ul>
  </footer>
</body>
</html>
